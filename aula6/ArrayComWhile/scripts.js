let soma = new Array(5);
let resultado = 0;

while (soma.length < 5) {
    soma = Number(prompt('Digite um valor'));
}

resultado = soma[0] + soma[1] + soma[2] + soma[3] + soma[4] 

console.log(soma);
console.log(resultado);